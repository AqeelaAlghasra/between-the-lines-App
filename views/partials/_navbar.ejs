<nav>

    <a href="/">Home</a>

                        <% if(user) { %>
                            <% if(user.role==='Admin' ) { %>
                                <a href="/admin/dashboard">Dashboard</a>
                                <a href="/admin/orders">All Orders</a>
                                <a href="/admin/users">All Users</a>
                                <a href="/admin/books">All Books</a>
                                <% } else if (user.role==='customer' ){ %>
                                    <a href="/customers">Dashboard</a>
                                    <a href="/customers/<%= user._id %>/orders">My Orders</a>
                                    <a href="/customers/<%= user._id %>/wishList">My WishList</a>
                                    <% } else if (user.role==='author' ){ %>
                                        <a href="/authors">Dashboard</a>
                                        <a href="/authors/<%= user._id %>/books">My Books</a>
                                        <a href="/authors/<%= user._id %>/orders">My Orders</a>
                                        <% } %>
                                            <% if(user.role==='Admin' ) { %>
                                                <a href="/admin/profile"> Profile</a>
                                                <% } else if (user.role==='customer' ){ %>
                                                    <a href="/customers/<%= user._id %>/profile"> Profile</a>
                                                    <% } else { %>
                                                        <a href="/authors/<%= user._id %>/profile"> Profile</a>
                                                        <% } %>
                                                            <a href="/auth/sign-out">Sign Out</a>
                                                            <% } else { %>
                                                                
                                                                <a href="/auth/sign-in">Sign In</a>
                                                                <a href="/auth/sign-up">Sign Up</a>
                                                                <% } %>

</nav>